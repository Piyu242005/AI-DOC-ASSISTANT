apiVersion: v1
kind: Service
metadata:
  name: neuraflow-ai-service
  namespace: default
  labels:
    app: neuraflow-ai
spec:
  type: ClusterIP
  ports:
  - port: 8501
    targetPort: 8501
    protocol: TCP
    name: http
  selector:
    app: neuraflow-ai
