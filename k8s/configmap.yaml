apiVersion: v1
kind: ConfigMap
metadata:
  name: neuraflow-config
  namespace: default
data:
  # General configuration that is not sensitive
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
  # STREAMLIT_SERVER_MAX_UPLOAD_SIZE: "200"
