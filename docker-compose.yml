version: '3.8'

services:
  neuraflow:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/venv
      - /app/.venv
    restart: unless-stopped
